// Created the icon component for Dexel the monster teacher.
import React from 'react';
import { DexelMascot } from '../DexelMascot';

export const Dexel: React.FC<{ className?: string }> = ({ className }) => {
    return <DexelMascot className={className} />;
};
